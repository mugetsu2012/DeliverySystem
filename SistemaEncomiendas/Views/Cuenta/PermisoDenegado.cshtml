
@{
    ViewData["Title"] = "Permiso Denegado";
    Layout = "_LayoutLogin";
}

<div class="offset-md-8 col-md-4 col-sm-4">
    <div class="card card-login card-hidden">
        <div class="card-header card-header-rose text-center">
            <h4 class="card-title">Acceso denegado</h4>

        </div>
        <div class="card-body">
            <div class="row">
                <div class="offset-md-2 col-md-10">
                    <p>No tienes acceso a ver esta pagina</p>
                </div>
            </div>
        </div>
        <div class="card-footer justify-content-center">
            <a href="@Url.Action("RedireccionarUsuario")" class="btn btn-rose btn-link btn-lg">Regresar</a>
        </div>
    </div>
</div>

